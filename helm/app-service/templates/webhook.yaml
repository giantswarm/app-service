apiVersion: admissionregistration.k8s.io/v1beta1
kind: MutatingWebhookConfiguration
metadata:
  name: {{ include "resource.default.name" . }}
  namespace: {{ include "resource.default.namespace" . }}
  annotations:
    cert-manager.io/inject-ca-from: {{ include "resource.default.namespace" . }}/{{ include "resource.default.name" . }}-certificates
  labels:
    {{- include "labels.common" . | nindent 4 }}
webhooks:
  - name: app.{{ include "resource.default.name" . }}.giantswarm.io
    failurePolicy: Ignore
    clientConfig:
      service:
        name: {{ include "resource.default.name" . }}
        namespace: {{ include "resource.default.namespace" . }}
        path: /webhooks/mutator
      caBundle: Cg==
    rules:
      - apiGroups: ["application.giantswarm.io"]
        resources:
          - "apps"
        apiVersions:
          - "v1alpha1"
        operations:
          - CREATE
          - UPDATE
